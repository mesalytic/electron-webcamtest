/**
 * TODO:
 * Get checkboxes, get email, and add database input
 * TODO:
 * Also redirect
 * */

document.addEventListener('DOMContentLoaded', () => {
    var button = document.getElementById("test");

    button.addEventListener("click", () => {
        alert("see ya later");
    })
})